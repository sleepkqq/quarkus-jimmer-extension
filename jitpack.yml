before_install:
  - sdk install java 21.0.12-tem
  - sdk use java 21.0.12-tem
install:
  - ./gradlew clean build publishToMavenLocal -Pversion=$VERSION -x :integration-tests:test -x :integration-tests:quarkusBuild
